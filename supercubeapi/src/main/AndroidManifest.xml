<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="fi.vexu.supercubeapi">
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>

    <application>
        <service android:name=".SuperCubeService"/>
    </application>
</manifest>
